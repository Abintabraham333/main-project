rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEBUG MODE: Allow any authenticated user to read/write EVERYTHING
    // WARNING: Do not leave this in production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
