import 'package:flutter/material.dart';

class LodgeComplaintPage extends StatelessWidget {
  const LodgeComplaintPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Lodge Complaint"),
      ),
      body: const Center(
        child: Text("Lodge Complaint Screen"),
      ),
    );
  }
}
